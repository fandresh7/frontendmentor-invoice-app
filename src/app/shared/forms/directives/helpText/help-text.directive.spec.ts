import { HelpTextDirective } from './help-text.directive'

describe('HelpTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HelpTextDirective()
    expect(directive).toBeTruthy()
  })
})
