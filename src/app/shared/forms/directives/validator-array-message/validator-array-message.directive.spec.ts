import { ValidatorArrayMessageDirective } from './validator-array-message.directive'

describe('ValidatorArrayMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorArrayMessageDirective()
    expect(directive).toBeTruthy()
  })
})
