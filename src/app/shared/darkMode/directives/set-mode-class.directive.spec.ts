import { SetModeClassDirective } from './set-mode-class.directive'

describe('SetModeClassDirective', () => {
  it('should create an instance', () => {
    const directive = new SetModeClassDirective()
    expect(directive).toBeTruthy()
  })
})
