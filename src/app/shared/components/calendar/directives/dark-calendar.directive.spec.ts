import { DarkCalendarDirective } from './dark-calendar.directive'

describe('DarkCalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkCalendarDirective()
    expect(directive).toBeTruthy()
  })
})
