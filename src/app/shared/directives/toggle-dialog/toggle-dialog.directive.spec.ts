import { ToggleDialogDirective } from './toggle-dialog.directive'

describe('ToggleDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDialogDirective()
    expect(directive).toBeTruthy()
  })
})
