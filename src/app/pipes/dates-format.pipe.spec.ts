import { DatesFormatPipe } from './dates-format.pipe'

describe('DatesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DatesFormatPipe()
    expect(pipe).toBeTruthy()
  })
})
